pipeline {
    agent {
        docker {
            image 'node:8' 
            args '-p 8080:8081' 
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}